<nav class="navbar navbar-expand-lg navbar-light fixed-top" style="background-color:#a7c9dc">
    <a class="navbar-brand" href="/accueil">Accueil</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">

            {% if is_granted("ROLE_USER") %}
                <a class="nav-item nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
            {% else %}
                <a class="nav-item nav-link" href="/specialites">Le SI</a>
                <a class="nav-item nav-link" href="/stage">Le Réseau</a>
                <a class="nav-item nav-link" href="{{ path('app_login') }}">Se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>
